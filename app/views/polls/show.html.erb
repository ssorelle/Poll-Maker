<h1><%= @poll.name %></h1>
<%= link_to "Edit", edit_poll_path(@poll) %> | <%= link_to 'Delete', @poll, method: :delete, data: { confirm: 'Are you sure?' } %>

<p><%= link_to 'Add some questions', new_poll_question_path(@poll) %><p>
	
<%= render :partial => "questions/index", :object => @poll.questions %>
